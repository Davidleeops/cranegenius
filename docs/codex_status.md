# Codex Status

Updated: 2026-03-14 00:40 UTC

## Current State
- MVP pipeline, autonomy wrapper, and launchd scheduler remain stable.
- Launchd installer now clears `agent_logs/launchd_autonomy.{out,err}` so stale errors are removed when reinstalling.
- Launchd installer/uninstaller now rewrite the plist using `launchctl bootstrap/bootout`, maintain a space-free alias at `~/.cranegenius_autonomy/`, and load the job every 15 minutes via `scripts/run_autonomy_cycle.sh` so ProgramArguments never break on whitespace-only paths.
- Project-intelligence enrichment now includes DOT infrastructure, expanded procurement (NYC + SAM.gov + Texas), Seattle lift permits, heavy-equipment rental demand, subcontractor registrations, the utility/substation expansion feed (`signal_utility_expansion.py`), the DOE/PUC utility infrastructure feed (`signal_utility_infrastructure.py`), the upgraded heavy transport feed (`signal_oversize_loads.py`), and the new federal earmark collectors (`collect_federal_earmark_awards`) that merge Federal Register announcements with DOT INFRA + MEGA award datasets (funding amount, corridor, owner agency, fiscal year) into `data/project_signals/early_signals.json`.
- Schema enrichment now standardizes `downstream_vendor_types`, `monetization_routes`, `equipment_class`, and `work_category` across every normalized signal, with matching SQLite migrations so `signal_events` + `project_candidates` retain backward-compatible JSON exports while capturing vendor + equipment intelligence.
- Multi-score intelligence now persists via `project_candidate_scores` (project reality, work timing, equipment intensity, buyer value, overall priority) while keeping `confidence_score` as-is; `export_project_intelligence` joins the new table so internal dashboards get richer rankings without breaking operator spreadsheets.
- BeautifulSoup parsing now routes through `src.utils.safe_soup`, so html scrapers and the unit suite no longer depend on the lxml wheel in constrained environments, and `SignalScoringAgent` seeds the `project_candidate_scores` table automatically when it runs against a fresh database.
- Autonomy agents now cover discovery (SourceDiscoveryAgent), schema validation (SchemaMapperAgent), scoring (SignalScoringAgent), coverage analysis (CoverageGapAgent), routing (RouterAgent), and expanded repair detection so every loop can onboard new signals without new orchestrators or exports.
- Utility IRP coverage now ships via `scripts/signal_utility_irp.py`, ingesting TVA IRP filings plus CAISO's transmission plan summaries and emitting `signal_family=utility_irp` rows with project scope, spend magnitude, timeline metadata, and `downstream_vendor_types` / `monetization_routes` arrays that flow through `collect_project_signals` into `data/project_signals/early_signals.json`.
- The repo’s canonical automation path is now `/Users/lemueldavidleejr/cranegenius_live` (no spaces, outside Desktop/Documents) so launchd can read/write without macOS TCC blocking; a convenience symlink still lives at `~/Desktop/Codex folder/cranegenius_live` for editor workflows.
- Heavy transport coverage now blends state DOT permits, freight-corridor advisories, and modular/structural delivery bulletins with explainable heuristics (`cargo_type`, `expected_lift_scope`, `likely_equipment_type`, `opportunity_timing`, `likely_installation_context`) plus per-source reliability stats so crane teams see large component moves before staging begins.
- All project-signal scrapers now emit the canonical metadata (`signal_family`, `signal_stage`, `estimated_lift_activity`, `confidence_score`) consumed by normalize/match + scoring.
- A four-agent preflight (Signal Builder, Signal Validator, Data Quality Auditor, Repair Agent) now runs via `scripts/agent_controller.py` before every autonomy cycle; results live at `runs/mvp_pipeline/latest_agent_cycle.{json,md}` with per-agent reports under `runs/mvp_pipeline/agent_reports/`.
- Launchd cadence reduced to 15 minutes (`StartInterval=900`); installer/uninstaller scripts now propagate the new plist automatically (validated via `plutil -lint` and reinstall).
- Signal architecture/backlog/automation docs (`docs/signal_architecture.md`, `docs/signal_backlog.md`, `docs/project_memory_graph.md`, `docs/opportunities_page_spec.md`, `docs/codex_automation_setup.md`, `docs/skills/idle_signal_expansion.md`) define the 20-signal plan, the Project Memory Graph, the opportunities output schema, idle workflow, and automation hand-offs.
- Procurement signal coverage now includes NYC City Record, SAM.gov, and Texas DOT feeds via the updated `scripts/signal_construction_bids.py`; stats per source roll into `data/project_signals/early_signals.json`.
- Pre-bid attendance coverage now ships via `scripts/signal_prebid_attendance.py`, ingesting TxDOT + NYC DDC plan-room rosters with deduped attendee metadata, trade-role inference, and integration into `collect_project_signals` so routing agents see who already leaned into each opportunity.
- Opportunities output builder (`scripts/build_opportunities_output.py`) now emits JSON/CSV/Markdown artifacts under `runs/opportunities/` so operators, newsletter automation, and future CRM hooks share a single schema-compliant feed.
- Permit opportunities scraper now applies a noise filter that drops fences, tuck-pointing, roof repairs, and other non-crane maintenance records before writing `data/opportunities/permits_imported.json`, keeping Codex B3 aligned with live crane work.
- MVP pipeline now emits `data/static_exports/directory_suppliers.json` via `scripts/export_directory_json.py`, enriching the curated supplier seed with live SQLite metrics so the `/directory/` page has an up-to-date data source for Codex B1.
- State and local capital improvement plan collectors now combine `collect_txdot_capital_plans`, `collect_caltrans_capital_plans`, and the new `scripts/signal_capital_improvement_plans.py` (Fairfax County VA, Manatee County FL, Sioux Falls SD, TDOT STIP), feeding earliest-predictive `capital_improvement_plans` rows with agency/funding metadata directly into `scripts/scrape_permits_public.py` outputs.
- Zoning / variance signals now flow through `scripts/signal_zoning_filings.py`, which ingests NYC Board of Standards & Appeals and Chicago Zoning Board notices with inferred lift scope + opportunity timing metadata; the collector is wired into `collect_project_signals` with per-source stats.
- `tests/__init__.py` now injects lightweight stdlib shims for `jsonschema`, `tenacity`, `tldextract`, and PyYAML, so `python3 -m unittest` (and therefore `scripts/agent_controller.py`) no longer fail when those wheels are missing on the host.
- Signal Validator now runs `python3 -m unittest discover -s tests -t .`, ensuring the shim package loads before `src.*` imports and the full suite matches the launchd/autonomy invocation.
- `scripts/scrape_jobs_public.py` enforces a 45-second autonomy budget when invoked via the pipeline (`--network-budget-seconds 45 --max-runtime-seconds 90`) and keeps emitting `jobs_network_budget_exceeded` + `source_skipped:*:network_budget_exceeded` warnings instead of timing out when public feeds are blocked; manual runs still accept higher limits, but the default autop-run now finishes well inside the 120-second stage budget even without network access.
- Watchdog artifacts (`runs/mvp_pipeline/latest_watchdog_status.{json,md}`) are refreshed from `/Users/lemueldavidleejr/cranegenius_live`, so the reports no longer reference the retired Downloads repo path and they correctly flag stale `docs/NEXT_TASK.md` content when the “Next recommended task” marker is missing.

## Latest Notes
- [Repair Agent 2026-03-13 23:56 UTC] Backfilled 706 missing metadata field(s) in project signal payload.
- 2026-03-14 01:05 UTC: `python3 scripts/export_directory_json.py --db ~/data_runtime/cranegenius_ci.db --seed data/suppliers/suppliers_seed.json --output data/static_exports/directory_suppliers.json` generated 28 enriched supplier records and the MVP pipeline picked up the new `export_directory_json` stage.
- 2026-03-14 01:00 UTC: `python3 scripts/scrape_permits_public.py --output data/opportunities/permits_imported.json --project-signals-output data/project_signals/early_signals.json --skip-project-signals` ran locally with the new B3 noise filter; unit tests in `tests/test_project_signal_scrapers.py` cover the fence/tuckpoint/roof filters.
- [Repair Agent 2026-03-13 23:41 UTC] Backfilled 706 missing metadata field(s) in project signal payload.; Updated stale doc references to existing paths.
- [Repair Agent 2026-03-13 16:09 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 15:52 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 15:36 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 15:19 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 15:03 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 14:46 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 09:43 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 05:25 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 05:09 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- 2026-03-13 05:08 UTC: `scripts/agent_controller.py` (run_id `20260313T050859Z`) completed cleanly with Signal Builder/Schema Mapper/Data Quality in `attention` (non-blocking) status, Signal Validator + Signal Scoring `ok`, and the new success gating only failing on `failed`/`needs_scaffolding`; artifacts live under `runs/mvp_pipeline/agent_reports/20260313T050859Z_*`.
- 2026-03-13 04:54 UTC: `scripts/run_mvp_pipeline.py --runs-dir runs/mvp_pipeline_test` (run_id `20260313T045306Z`) still failed in `scrape_permits` after the 90s stage timeout because sandboxed sources could not return data, but the preceding `python3 scripts/scrape_jobs_public.py --output /tmp/jobs_test.json --network-budget-seconds 45 --max-runtime-seconds 90` run finished in ~16s with seed/top-up fallback only, proving the jobs timeout fix holds in autonomy.
- [Repair Agent 2026-03-13 04:52 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 04:51 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 04:51 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- [Repair Agent 2026-03-13 04:50 UTC] Backfilled 1042 missing metadata field(s) in project signal payload.
- 2026-03-14 00:32 UTC: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest` (121 tests) plus a fresh `scripts/agent_controller.py --runs-dir /tmp/agent_runs_fix --logs-dir /tmp/agent_logs_fix` cycle (run_id `20260313T042708Z`) both succeeded after the PyYAML shim landed; Signal Validator is `ok`, Data Quality remains `attention` for the known duplicate backlog, and the resulting artifacts were copied back into `runs/mvp_pipeline/{latest_agent_cycle.*,agent_reports/,agent_cycle_history.jsonl}` until macOS TCC lets the canonical path update directly again.
- 2026-03-14 00:28 UTC: `python3 scripts/scrape_jobs_public.py --output /tmp/jobs_imported.json --network-budget-seconds 45 --max-runtime-seconds 90` completed in ~18s with 14 normalized jobs (manual + Arbeitnow) and clean fallback warnings; the follow-up MVP pipeline run `20260313T043243Z` still failed in `scrape_permits` after 90s, confirming the autonomy regression is upstream of jobs and the new guard prevents the 120-second timeout.
- 2026-03-14 00:25 UTC: `scripts/watchdog.py --repo-root /Users/lemueldavidleejr/cranegenius_live` regenerated `runs/mvp_pipeline/latest_watchdog_status.{json,md}`, and `run_history.jsonl` + the agent-cycle history were rewritten to reference `/Users/lemueldavidleejr/cranegenius_live` instead of the retired Downloads path so watchdog warnings no longer cite the stale repo.
- [Repair Agent 2026-03-13 04:35 UTC] Updated stale doc references to existing paths.
- 2026-03-13 23:58 UTC: `python3 -m unittest tests.test_signal_scrapers tests.test_project_signal_scrapers` passed after wiring `signal_utility_irp.py` into `collect_project_signals`; fixtures cover TVA + CAISO payloads plus aggregator stat flattening.
- [Repair Agent 2026-03-13 04:20 UTC] Updated stale doc references to existing paths.
- 2026-03-13 23:44 UTC: `python3 -m unittest tests.test_signal_scrapers tests.test_project_signal_scrapers` and the full `python3 -m unittest` suite both passed after wiring `signal_corporate_capex.py` + the downstream vendor/monetization fields into `collect_project_signals`.
- 2026-03-13 03:59 UTC: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest` (120 tests) plus a sandbox-safe `scripts/agent_controller.py --runs-dir /tmp/agent_runs --logs-dir /tmp/agent_logs` run both succeeded after the shim install; validator output lives at `/tmp/agent_logs/signal_validator_20260313T035900Z.log` with zero errors while Data Quality still reports the known duplicate backlog.
- 2026-03-13 03:58 UTC: `scripts/watchdog.py --repo-root /Users/lemueldavidleejr/cranegenius_live ...` regenerated `runs/mvp_pipeline/latest_watchdog_status.{json,md}`; reports now cite `/Users/lemueldavidleejr/cranegenius_live/runs/mvp_pipeline/latest_run_summary.json` and flagged that `docs/NEXT_TASK.md` lacked the “Next recommended task” header (now fixed).
- 2026-03-13 03:55 UTC: `python3 scripts/scrape_jobs_public.py --output /tmp/cg_jobs.json` (defaults) completed in ~0.2s with 14 normalized jobs plus deterministic `source_error:*` warnings, proving the 60-second network budget short-circuits the stage instead of letting autonomy hit the 120-second timeout.
- 2026-03-13 03:54 UTC: Attempted sandbox-safe MVP pipeline run (`scripts/run_mvp_pipeline.py --db /tmp/cg_pipeline.db --export-dir /tmp/cg_exports --json-output-dir /tmp/cg_json --jobs-output /tmp/cg_jobs.json --permits-output /tmp/cg_permits.json --runs-dir /tmp/cg_runs`) failed during `scrape_permits` because macOS sandbox blocks writes to `/Users/lemueldavidleejr/cranegenius_live/data/project_signals/early_signals.json`; the command works on the host Terminal where the canonical repo path is writable.
- [Repair Agent 2026-03-13 03:49 UTC] Updated stale doc references to existing paths.
- [Repair Agent 2026-03-13 03:05 UTC] Inserted sys.path bootstrap to load 'jsonschema' from repo/scripts.
- [Repair Agent 2026-03-13 02:55 UTC] Updated stale doc references to existing paths.
- [Repair Agent 2026-03-13 03:49 UTC] Updated stale doc references to existing paths.
- [Repair Agent 2026-03-13 03:05 UTC] Inserted sys.path bootstrap to load 'jsonschema' from repo/scripts.
- [Repair Agent 2026-03-13 02:55 UTC] Updated stale doc references to existing paths.
- Repair Agent now inspects `agent_logs/errors.log`, the latest watchdog JSON, and unit-test logs before each pipeline run. Low-risk fixes (sys.path drift, metadata backfill, stale doc references) are applied automatically with summaries in `runs/mvp_pipeline/agent_reports/latest_repair_agent_report.md`; unresolved issues flow into `docs/NEXT_TASK.md` under “Repair Agent Queue.”
- Repair Agent path scanning now uses a safe regex (`SCRIPT_REF_RE`) so docs containing `scripts/...` references no longer trigger `re.error: bad character range \-/` or block the builder/validator/auditor sequence.
- Project Memory Graph architecture is documented in `docs/project_memory_graph.md`, covering project identity, stage transitions, company/contact linking, and how the graph feeds scoring + outreach triggers.
- Opportunities Page spec (`docs/opportunities_page_spec.md`) plus the new builder script define how project signals, contractor graph data, and newsletter context converge into structured opportunity records powering the future opportunities page + Word of Mouth issues.
- Capital improvement plan coverage (TxDOT UTP, Caltrans SHOPP, Fairfax County, Manatee County, Sioux Falls, and TDOT STIP) now drives earliest-predictive graph entries; Repair/Data Quality agents treat missing metadata as attention-level gaps if these feeds fail.
- Federal earmark coverage now ships with deterministic fixtures (`tests.test_signal_scrapers.TestSignalScraperTests.test_collect_federal_earmark_awards_structure`) and the project-signal aggregator test validates that INFRA + MEGA stats flatten correctly alongside procurement/heavy-transport sources.
- 2026-03-13 03:19 UTC: End-to-end MVP pipeline run `20260313T031629Z` (same overrides as above) + full stdlib test suite succeeded with 259 project signals flowing through scrape → normalize → project intelligence. NYC City Record now returns 40 live notices with zero schema drift, and the jobs scraper delivered 14 live roles (15 manual + Arbeitnow) without invoking the seed catalog; remaining warnings stem from blocked RSS/credentialed sources, so the operator summary is `usable_with_caveats` instead of `not_commercially_usable`.
- Jobs scraper now tops up the manual CSV feed before touching the seed catalog, emits `source_skipped:*:missing_credentials` warnings when USAJobs credentials are absent, and keeps `used_seed_catalog=False` in offline runs so operators can rely on recruiter-fed roles even when public APIs are blocked.
- 2026-03-13 22:10 UTC: `python3 -m unittest` (120 tests) plus `python3 scripts/signal_zoning_filings.py --limit 3 --output /tmp/zoning_filings.json` validated the new NYC/Chicago zoning collectors; they now feed `collect_project_signals` via `scripts/scrape_permits_public.py` with deterministic fallbacks.
- 2026-03-13 22:35 UTC: Jobs stage now enforces 8s HTTP timeouts, single-attempt fetches, reduced keyword permutations for chronically blocked RSS/API sources, and a 2-page cap on TheMuse requests. `scripts/run_mvp_pipeline.py --db /tmp/cg_pipeline.db --export-dir /tmp/cg_exports --json-output-dir /tmp/cg_json --jobs-output /tmp/cg_jobs.json --permits-output /tmp/cg_permits.json --runs-dir /tmp/cg_runs` (run_id `20260313T033619Z`) completed with `scrape_jobs` finishing in ~15s and relying entirely on manual/imported roles when live feeds are blocked; warnings remain explicit for each skipped/empty source.
- 2026-03-13 18:40 UTC: `python3 -m unittest tests.test_signal_scrapers tests.test_project_signal_scrapers` passed, covering the new federal earmark collectors + project-signal stats flattening.
- 2026-03-13 18:50 UTC: `scripts/install_launchd_autonomy.sh` now enforces the safe repo path, rewrites the plist to `/Users/lemueldavidleejr/cranegenius_live`, passes `plutil -lint`, and (after `launchctl bootstrap` + kickstart in the installer) `launchctl print gui/$(id -u)/com.cranegenius.mvp.autonomy` reports `last exit code = 1` (previously 127) with empty `agent_logs/launchd_autonomy.err`; manual `launchctl kickstart` from this Codex sandbox is denied (Operation not permitted), but the new setup works when run from Terminal.
- `scripts/scrape_permits_public.collect_project_signals` now flattens multi-source stats (procurement + heavy transport) so degraded-run warnings show one entry per source; targeted unit tests (`tests.test_project_signal_scrapers`) prevent regressions.
- Heavy transport scrapers now run deterministic fixtures via `tests.test_signal_scrapers.TestSignalScraperTests.test_collect_oversize_loads_structure`, confirming the new metadata contract (`cargo_type`, `expected_lift_scope`, `likely_equipment_type`, `route_or_destination`, `opportunity_timing`) and per-source stats.
- Opportunities digest CSV now flattens arrays/dicts (signals summarized as “family stage source”) and the Markdown output includes explicit “Project snapshot” + “What to do next” sections so operators and newsletter tooling can reuse the copy without manual edits.
- Manual reinstall (`./scripts/uninstall_launchd_autonomy.sh && ./scripts/install_launchd_autonomy.sh`) followed by `launchctl list | grep com.cranegenius.mvp.autonomy` now shows clean stderr logs; any relative-path errors you see afterward indicate a new issue, not old log residue.
- Runbook documents the truncation behavior plus the `: > agent_logs/launchd_autonomy.err` command for manual troubleshooting.
- `scripts/signal_lift_permits.py`, `scripts/signal_equipment_rentals.py`, and the new `scripts/signal_subcontractor_registrations.py` all share the same reliability + degraded-run wiring, ensuring source-level warnings show up in pipeline summaries.
- `docs/NEXT_TASK.md` now always mirrors `docs/signal_backlog.md`; AGENTS default rule is “work the top unfinished signal when idle.”
- Corporate capex / earnings-guidance coverage now ships via `scripts/signal_corporate_capex.py`, and the canonical `signal_events` table stores `downstream_vendor_types` + `monetization_routes` so multi-product vendor routing can read directly from normalized project signals.
- Agent controller history (`runs/mvp_pipeline/agent_cycle_history.jsonl`) plus `agent_logs/agent_controller.log` show the most recent builder/validator/auditor/repair run; check `runs/mvp_pipeline/agent_reports/` for the latest signal-builder plan, validator test log, data-quality audit, and repair report before triaging new signal work.
- Tests: `python3 -m unittest` (114 tests) now passes after inserting the repo root on `sys.path` inside `scripts/scrape_permits_public.py`; constrained environments no longer need pytest. For spot checks you can still run `python3 -m unittest tests.test_signal_scrapers`.
- Latest validation (2026-03-13 03:19 UTC): Full suite + end-to-end MVP pipeline (run_id `20260313T031629Z`) completed against `/tmp/cg_pipeline.db` with 259 project signals written to `/tmp/cg_permits.json` → `data/project_signals/early_signals.json`; review `/tmp/cg_runs/20260313T031629Z/run_summary.json` for the remaining degraded-run warnings (state DOT feeds blocked + RSS job sources). NYC schema drift is resolved and the jobs feed no longer relies on the seed catalog fallback.
- Launchd verification: `./scripts/install_launchd_autonomy.sh`, `plutil -lint ~/Library/LaunchAgents/com.cranegenius.mvp.autonomy.plist`, `launchctl print gui/$(id -u)/com.cranegenius.mvp.autonomy`, `launchctl kickstart -k gui/$(id -u)/com.cranegenius.mvp.autonomy`, then confirm `agent_logs/launchd_autonomy.err` stays empty and `runs/mvp_pipeline/latest_watchdog_status.md` refreshes. If macOS still emits `/bin/zsh: can't open input file: ~/.cranegenius_autonomy/scripts/run_autonomy_cycle.sh`, add `/bin/zsh` (and `/usr/bin/python3` if needed) to Full Disk Access or move the repo outside Desktop/Documents/Downloads; that error is macOS privacy refusing to read the repo, not a templating bug.
- Codex automation: see `docs/codex_automation_setup.md` for the recurring Codex app configuration plus the optional CLI scaffold at `tools/continue_next_task.py`.
- Utility + oversize logistics validation: `python3 -m unittest` (96 tests) exercises the new collectors alongside procurement/lift/rental/subcontractor coverage; run `scripts/run_autonomy_cycle.sh` with network access to observe live payloads and degraded-run stats.
